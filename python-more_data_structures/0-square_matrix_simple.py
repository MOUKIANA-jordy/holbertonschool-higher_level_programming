#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_matrix = []
    for row in matrix:
        K = []
        for column in row:
            a.append(column * column)
        new_matrix.append(K)
    return new_matrix
